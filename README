install using:
  git clone https://github.com/PyGamer0/vimdot
  cd vimdot && ./install

features:
  lsp!  (neovim only)
  no treesitter :(  (uses vim-polygot...)
  similar configuration for vim and neovim (they use the same file for config...)
  
bugs:
  the statusline broken on nvim...
